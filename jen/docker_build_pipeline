pipeline{

    agent any

    stages{

        stage('Build Docker image'){
            steps{
                sh 'docker build -t myapp:latest: .'
            }
        }
        stage('show image'){
            steps{
                sh 'docker images'
            }
        }
    }
}